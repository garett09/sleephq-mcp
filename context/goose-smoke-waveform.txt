Contextual waveform windows — smoke test only. Do NOT write a clinical report.

Use TEST_DATE = 2026-05-19 (or your newest CPAP night with BRP).

Important — maxMinutes:
- Do NOT pass maxMinutes unless you want a smaller slice. Omit it so the server uses your .env defaults.
- First call get-configured-defaults and note mcp_payload_hints.waveform_default_max_minutes (expect 480 if .env has SLEEPHQ_MCP_WAVEFORM_DEFAULT_MAX_MINUTES=480).

Call these MCP tools in order:
0. get-configured-defaults
1. get-waveform-by-date(date=TEST_DATE, anchor=auto)
2. get-waveform-by-date(date=TEST_DATE, anchor=worst_leak)
3. get-waveform-by-date(date=TEST_DATE, startMinute=<start_minute from call 1>, maxMinutes=15)
4. get-combined-night-by-date(date=TEST_DATE)

PASS/FAIL only (no clinical interpretation).

Checks:
- Step 0: mcp_payload_hints shows your waveform_default_max_minutes and cap.
- Calls 1–2: omit maxMinutes; window_selection.max_minutes should match server default; anchor rules as before.
- Call 3: manual override with maxMinutes=15 only to keep response smaller.
- Call 4: journal.sleep_stages_summary must include summary_schema_version (>=3), legend_profile, minutes_by_stage_for_reporting, reporting_source. If summary_schema_version < 3, FAIL — restart ./run.sh. For your TEST_DATE expect legend_profile=sleephq_dashboard and reporting minutes near what your SleepHQ UI shows (rem/awake/deep/core in minutes).

Reply with:
1) markdown table: step | PASS/FAIL | note
2) compact JSON: test_date, configured_payload_hints, auto_window_selection, worst_leak_window_selection, journal_sleep_stages_summary

Full checklist: docs/smoke-test-waveform-windows.md
